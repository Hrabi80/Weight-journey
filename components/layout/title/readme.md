# Title

Heading primitive with independent `order` (h1-h6) and `size`.

```tsx
import { Title } from "../title/Title";

export function Example() {
  return (
    <Title order={2} size="h1" fw="bold">
      Section Heading
    </Title>
  );
}
```
